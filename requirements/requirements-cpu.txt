# SIGO - CPU-Only Configuration
# For systems without NVIDIA GPU or older hardware
# Updated: February 2026

# PyTorch CPU version (much smaller download)
# Install with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0

# Core dependencies
opencv-python>=4.10.0,<5.0.0
ultralytics>=8.3.0,<9.0.0
openai>=1.55.0,<2.0.0
faster-whisper>=1.1.0
numpy>=1.26.0
scipy>=1.14.0
sounddevice>=0.5.1
pyserial>=3.5
keyboard>=0.13.5
pywin32>=308; sys_platform == 'win32'

# CPU optimization (still helps!)
numba>=0.60.0
