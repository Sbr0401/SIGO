# SIGO - High-Performance Configuration
# Optimized for maximum speed (requires NVIDIA GPU)
# Updated: February 2026
#
# Install PyTorch with CUDA first:
#   RTX 50-series: pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
#   RTX 20/30/40:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# Core dependencies
opencv-python>=4.10.0,<5.0.0
ultralytics>=8.3.0,<9.0.0
openai>=1.55.0,<2.0.0
faster-whisper>=1.1.0
numpy>=1.26.0
scipy>=1.14.0
sounddevice>=0.5.1
pyserial>=3.5
keyboard>=0.13.5
pywin32>=308; sys_platform == 'win32'

# Performance boosters
numba>=0.60.0                         # 10-100x speedup for NumPy operations

# Optional (uncomment if needed)
# onnxruntime-gpu>=1.19.0             # Faster YOLO inference
# rich>=13.9.0                        # Beautiful console output
